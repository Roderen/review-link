# 🔄 Workflow разработки и деплоя

Гайд по правильному процессу разработки, деплою и откату изменений.

---

## 📝 Правильный workflow разработки

### Вариант 1: Деплой из main (рекомендуется с GitHub Actions)

```bash
# 1. Работайте в feature branch
git checkout -b feature/new-feature

# 2. Вносите изменения
# ... code code code ...

# 3. Тестируйте локально
npm run dev

# 4. Commit изменения
git add .
git commit -m "feat: Add new feature"

# 5. Push в feature branch
git push origin feature/new-feature

# 6. Создайте Pull Request на GitHub

# 7. После мержа в main → GitHub Actions автоматически задеплоит!
```

**Преимущества:**
- ✅ Автоматический деплой при merge в main
- ✅ Всегда деплоится протестированный код
- ✅ История деплоев = история коммитов в main

### Вариант 2: Ручной деплой из любой ветки (для быстрых фиксов)

```bash
# 1. Убедитесь что на правильной ветке
git branch

# 2. Соберите проект
npm run build

# 3. Деплой
firebase deploy

# ИЛИ деплой только части:
firebase deploy --only hosting        # Только frontend
firebase deploy --only functions       # Только backend
firebase deploy --only firestore:rules # Только правила БД
```

**Когда использовать:**
- ⚠️ Критический hotfix (срочный баг на проде)
- 🧪 Тестирование на staging окружении
- 🚀 Первый деплой проекта

**Важно:**
```bash
# После ручного деплоя НЕ ЗАБУДЬТЕ:
git add .
git commit -m "fix: Critical bug"
git push origin your-branch

# И создать PR чтобы изменения попали в main!
```

---

## 🔙 Как откатиться на предыдущую версию

### Способ 1: Откат через Firebase Console (САМЫЙ ПРОСТОЙ) ⭐

**Когда использовать:** Быстрый откат frontend (hosting) за 30 секунд.

**Шаги:**

1. Откройте Firebase Console:
```bash
firebase open
```

2. **Hosting** → **Выберите ваш сайт**

3. **Release history** (История релизов)

4. Найдите рабочую версию:
```
✅ Version abc123 - 2 hours ago (working)
❌ Version def456 - 10 minutes ago (broken)
```

5. Нажмите **⋮** (три точки) → **Rollback**

6. Подтвердите откат

**Результат:** Сайт откатится за 10-30 секунд!

**Ограничения:**
- ✅ Работает для Hosting (frontend)
- ❌ НЕ работает для Cloud Functions
- ❌ НЕ работает для Firestore Rules

---

### Способ 2: Откат через Git + передеплой

**Когда использовать:** Откат backend, функций, или всего проекта целиком.

#### A) Откат на конкретный коммит:

```bash
# 1. Посмотрите историю коммитов
git log --oneline -10

# Вывод:
# e137ba1 feat: Add Firebase security rules
# f0095dc docs: Add custom domain setup guide
# 95718f0 docs: Add comprehensive project analysis  ← СЮДА ХОТИМ ОТКАТИТЬСЯ
# 650edb9 Merge pull request #44

# 2. Создайте новую ветку от нужного коммита
git checkout -b rollback/fix-issue 95718f0

# 3. Деплой старой версии
npm run build
firebase deploy

# 4. Если все работает - запушьте
git push origin rollback/fix-issue

# 5. Создайте PR для мержа в main
```

#### B) Откат последнего коммита:

```bash
# 1. Отмените последний коммит (сохранив изменения)
git reset --soft HEAD~1

# 2. Или отмените последний коммит (удалив изменения)
git reset --hard HEAD~1

# 3. Деплой
npm run build
firebase deploy

# 4. Force push (ОСТОРОЖНО! только если уверены)
git push --force origin your-branch
```

⚠️ **Внимание:** `git reset --hard` **удаляет изменения навсегда**!

#### C) Revert коммита (безопасный способ):

```bash
# 1. Создайте "обратный коммит"
git revert HEAD

# Это создаст новый коммит который отменяет последний
# История сохраняется!

# 2. Деплой
npm run build
firebase deploy

# 3. Push
git push origin your-branch
```

**Разница:**
- `reset` - перемещает HEAD назад (переписывает историю)
- `revert` - создает новый коммит отменяющий изменения (история сохраняется)

**Рекомендуется:** Используйте `revert` для безопасности!

---

### Способ 3: Откат Cloud Functions

**Проблема:** Firebase Console не умеет откатывать функции.

**Решение:**

```bash
# 1. Откатите код через git (см. Способ 2)
git checkout 95718f0 -- functions/

# 2. Пересоберите функции
cd functions
npm run build
cd ..

# 3. Деплой только функций
firebase deploy --only functions

# 4. Проверьте логи
firebase functions:log
```

**Или удалите сломанную функцию:**

```bash
# Удалить конкретную функцию
firebase functions:delete brokenFunction

# Задеплоить старую версию
firebase deploy --only functions
```

---

### Способ 4: Откат Firestore Rules

**Если новые правила сломали доступ к БД:**

```bash
# 1. Откатите файл rules
git checkout HEAD~1 -- firestore.rules

# 2. Деплой только правил (быстро!)
firebase deploy --only firestore:rules

# 3. Проверьте в Firebase Console
firebase open
# Firestore Database → Rules → Проверьте версию
```

---

## 🚨 Экстренный откат за 1 минуту

**Ситуация:** Продакшн сломался, нужно откатить ПРЯМО СЕЙЧАС!

### Frontend (Hosting):

```bash
# Способ 1: Firebase Console (30 секунд)
firebase open
# → Hosting → Release history → Rollback

# Способ 2: CLI
firebase hosting:clone SOURCE_SITE_ID:SOURCE_VERSION DESTINATION_SITE_ID
```

### Backend (Functions):

```bash
# Откатите через git и передеплойте
git stash                    # Сохранить текущие изменения
git checkout main            # Переключиться на стабильную ветку
firebase deploy --only functions

# Потом вернитесь
git checkout your-branch
git stash pop
```

---

## 📊 Best practices для безопасного деплоя

### 1. Используйте staging окружение

**Создайте тестовый Firebase проект:**

```bash
# Добавьте staging проект
firebase use --add

# Выберите окружение
firebase use production  # Для прода
firebase use staging     # Для тестов

# Деплой в staging
firebase use staging
npm run build
firebase deploy

# Проверили что работает → деплой в production
firebase use production
firebase deploy
```

### 2. Деплойте по частям

```bash
# Сначала правила (быстро откатить если что)
firebase deploy --only firestore:rules

# Проверили → деплой функций
firebase deploy --only functions

# Проверили → деплой frontend
firebase deploy --only hosting
```

### 3. Делайте бэкапы перед большими изменениями

```bash
# Бэкап Firestore
firebase firestore:export gs://your-bucket/backup-$(date +%Y%m%d-%H%M%S)

# Или через gcloud (если настроен)
gcloud firestore export gs://your-bucket/backup
```

### 4. Используйте feature flags (для критичных фич)

```typescript
// В коде:
const NEW_FEATURE_ENABLED = import.meta.env.VITE_NEW_FEATURE === 'true';

if (NEW_FEATURE_ENABLED) {
  // Новая фича
} else {
  // Старая логика
}
```

Если что-то сломалось - просто меняете env переменную без передеплоя!

---

## 🔍 Как проверить что откат прошел успешно

### 1. Проверьте версию в Firebase Console:

```bash
firebase open
# → Hosting → Release history
# Убедитесь что активна правильная версия
```

### 2. Проверьте сайт:

```bash
# Откройте в приватном режиме (без кэша)
# Chrome: Ctrl + Shift + N
# Firefox: Ctrl + Shift + P

https://your-project.web.app
```

### 3. Проверьте логи функций:

```bash
firebase functions:log

# Не должно быть ошибок
```

### 4. Проверьте Firestore Rules:

```bash
# Firebase Console → Firestore → Rules
# Проверьте что правила корректные
```

---

## 📝 Чеклист деплоя (чтобы не нужен был откат!)

Перед `firebase deploy` выполните:

```bash
# ✅ 1. Код собирается без ошибок
npm run build

# ✅ 2. Линтер проходит
npm run lint

# ✅ 3. Functions собираются
cd functions && npm run build && cd ..

# ✅ 4. Локально все работает
npm run dev
# Проверьте основные функции в браузере

# ✅ 5. Коммит создан
git status
# Не должно быть uncommitted changes

# ✅ 6. Правильная ветка
git branch
# Должна быть main или нужная feature branch

# ✅ 7. Бэкап важных данных (для критичных обновлений)
firebase firestore:export gs://your-bucket/backup

# Теперь можно деплоить!
firebase deploy
```

---

## 💡 Pro tips

### 1. Алиасы для быстрого деплоя

Добавьте в `package.json`:

```json
{
  "scripts": {
    "deploy": "npm run build && firebase deploy",
    "deploy:hosting": "npm run build && firebase deploy --only hosting",
    "deploy:functions": "cd functions && npm run build && cd .. && firebase deploy --only functions",
    "deploy:rules": "firebase deploy --only firestore:rules,storage:rules",
    "rollback": "firebase hosting:clone"
  }
}
```

Теперь:
```bash
npm run deploy         # Полный деплой
npm run deploy:hosting # Только frontend
```

### 2. Храните важные коммиты в тегах

```bash
# Перед большим деплоем создайте тег
git tag -a v1.0.0 -m "Stable version before payment integration"
git push origin v1.0.0

# Откат на тег (если что-то сломалось)
git checkout v1.0.0
npm run build
firebase deploy
```

### 3. Используйте GitHub Releases

После успешного деплоя:
```bash
# На GitHub → Releases → Create new release
# Tag: v1.0.0
# Description: что изменилось
```

Теперь всегда знаете какая версия на проде!

---

## 📊 Сравнение способов отката

| Способ | Скорость | Сложность | Что откатывает | Когда использовать |
|--------|----------|-----------|----------------|-------------------|
| **Firebase Console** | 30 сек | Легко | Только Hosting | Сломался frontend |
| **git revert** | 2-5 мин | Средне | Все | Безопасный откат |
| **git reset** | 1-3 мин | Сложно | Все | Личная ветка |
| **Частичный деплой** | 1-2 мин | Легко | Выбранное | Сломалась 1 часть |

---

## ⚠️ Что НЕЛЬЗЯ делать

❌ **Force push в main:**
```bash
git push --force origin main  # НИКОГДА!
```

❌ **Удалять production БД:**
```bash
# Удаление документов без бэкапа
```

❌ **Деплоить из грязной ветки:**
```bash
git status
# Untracked files...
firebase deploy  # Плохая идея!
```

❌ **Тестировать на production:**
```bash
# Сначала протестируйте на staging!
```

---

## ✅ Итоговый workflow

**Обычная разработка:**
```
1. Feature branch → 2. Локальный тест → 3. Push →
4. PR → 5. Merge в main → 6. GitHub Actions деплоит
```

**Срочный hotfix:**
```
1. Checkout main → 2. Fix → 3. firebase deploy →
4. Commit → 5. Push → 6. PR
```

**Откат:**
```
Frontend: Firebase Console → Rollback
Backend: git checkout старый код → firebase deploy
```

---

**Главное правило:** Всегда можно откатиться! Firebase хранит историю версий. 🔄
