# 🚀 Настройка автоматического деплоя на Firebase

## Текущая конфигурация

Ваш проект уже настроен на Firebase и готов к деплою!

**Стек:**
- Frontend: React + TypeScript + Vite
- Backend: Firebase Cloud Functions
- База: Firestore + Firebase Storage
- Auth: Firebase Authentication

---

## ✅ Рекомендация: Firebase Hosting (бесплатно для MVP)

### Преимущества для вашего проекта:
- ✅ Уже настроен (`firebase.json`)
- ✅ Бесплатный tier: 125K вызовов функций/мес
- ✅ CDN + SSL автоматически
- ✅ Подходит для 3,000-5,000 пользователей/мес
- ✅ WayForPay уже интегрирован

---

## 📋 Шаги настройки автодеплоя

### 1. Получите Firebase Service Account

```bash
# В корне проекта выполните:
firebase init hosting:github

# Или вручную создайте service account:
# 1. Перейдите: https://console.firebase.google.com/project/_/settings/serviceaccounts/adminsdk
# 2. Нажмите "Generate new private key"
# 3. Скачайте JSON файл
```

### 2. Добавьте секреты в GitHub

Перейдите в Settings → Secrets and variables → Actions → New repository secret:

| Имя секрета | Значение | Где взять |
|-------------|----------|-----------|
| `FIREBASE_SERVICE_ACCOUNT` | Содержимое JSON файла | Из шага 1 |
| `VITE_FIREBASE_API_KEY` | Ваш API ключ | `.env` файл |
| `VITE_FIREBASE_AUTH_DOMAIN` | Auth домен | `.env` файл |
| `VITE_FIREBASE_PROJECT_ID` | ID проекта | `.env` файл |
| `VITE_FIREBASE_STORAGE_BUCKET` | Storage bucket | `.env` файл |
| `VITE_FIREBASE_MESSAGING_SENDER_ID` | Sender ID | `.env` файл |
| `VITE_FIREBASE_APP_ID` | App ID | `.env` файл |

### 3. Готово! 🎉

Теперь при каждом push в `main` будет:
1. Собираться frontend
2. Автоматически деплоиться на Firebase Hosting

**URL:** `https://your-project-id.web.app`

---

## 🔧 Ручной деплой (без GitHub Actions)

```bash
# 1. Установите Firebase CLI
npm install -g firebase-tools

# 2. Войдите в аккаунт
firebase login

# 3. Соберите проект
npm run build

# 4. Деплой hosting + functions
firebase deploy

# Или по отдельности:
firebase deploy --only hosting
firebase deploy --only functions
```

---

## 💰 Упрощение для MVP без платежей

Если вы хотите временно убрать платную подписку:

### Вариант 1: Отключить функции платежей (быстро)

```bash
# Закомментируйте в functions/src/index.ts:
// export { createWayForPayPayment } from './createWayForPayPayment';
// export { wayforpayWebhook } from './wayforpayWebhook';

# Оставьте только:
export { checkExpiredSubscriptions } from './checkExpiredSubscriptions';
```

### Вариант 2: Удалить папку functions (экономия)

```bash
# Удалите:
rm -rf functions/

# Обновите firebase.json (уберите секцию functions):
{
  "hosting": { ... },
  "firestore": { ... },
  "storage": { ... }
  // Удалите "functions": { ... }
}
```

**Экономия:** Без Cloud Functions проект будет работать только на клиенте + Firestore Rules.

---

## 📊 Мониторинг использования

Проверяйте лимиты бесплатного плана:
- Firebase Console → Usage and billing
- Alerts настраиваются автоматически

**Когда переходить на платный план:**
- Functions > 100K вызовов/мес
- Firestore > 40K чтений/день
- Storage > 5GB

---

## 🔄 Миграция на другой хостинг (если понадобится)

### Если вырастете из Firebase:

**Vercel/Netlify:**
- ❌ Нужна переписка Cloud Functions → Edge Functions
- ❌ Нужна смена БД (Firestore → PostgreSQL/MongoDB)
- 💰 Не выгодно для текущей архитектуры

**VPS (DigitalOcean/Hetzner):**
- ✅ Полный контроль
- 💰 От $4-5/мес
- ⚠️ Нужно настраивать все вручную (Docker, CI/CD, SSL)

**Рекомендация:** Оставайтесь на Firebase до 10,000+ пользователей.

---

## 🆘 Поддержка

Если что-то не работает:
1. Проверьте `firebase.json` конфигурацию
2. Посмотрите логи: `firebase functions:log`
3. Проверьте секреты в GitHub Actions

---

## 🎯 Краткий чеклист

- [ ] Настроить GitHub Secrets
- [ ] Создать Firebase Service Account
- [ ] Пушнуть в `main` для первого деплоя
- [ ] Проверить что сайт работает на `*.web.app`
- [ ] (Опционально) Настроить custom домен
- [ ] (Опционально) Отключить платежные функции для экономии
