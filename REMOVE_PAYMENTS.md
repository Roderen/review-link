# 💳 Удаление платежей для бесплатного MVP

## Зачем это нужно

Для тестирования MVP без платной подписки нужно:
1. Убрать интеграцию с WayForPay
2. Дать всем пользователям бесплатный доступ
3. Сэкономить на Cloud Functions

---

## 🎯 План действий

### Вариант 1: Минимальные изменения (рекомендуется)

**Что делаем:**
- Оставляем структуру кода
- Даем всем пользователям "Pro" план бесплатно
- Отключаем платежные функции

**Преимущества:**
- Легко вернуть платежи потом
- Не нужно переписывать код
- Быстро (5 минут)

### Вариант 2: Полное удаление

**Что делаем:**
- Удаляем все платежные функции
- Упрощаем UI (убираем страницы тарифов)
- Удаляем поля подписки из БД

**Преимущества:**
- Чище кодбаза
- Экономия на Cloud Functions
- Меньше сложности

---

## 📝 Вариант 1: Даем всем Pro план (быстро)

### Шаг 1: Отключите платежные функции

В файле `functions/src/index.ts`:

```typescript
import * as admin from 'firebase-admin';

admin.initializeApp();

// Закомментируйте платежные функции:
// export { createWayForPayPayment } from './createWayForPayPayment';
// export { wayforpayWebhook } from './wayforpayWebhook';

// Оставьте только:
export { checkExpiredSubscriptions } from './checkExpiredSubscriptions';
```

### Шаг 2: Измените checkExpiredSubscriptions

В файле `functions/src/checkExpiredSubscriptions.ts` добавьте логику "всем Pro":

```typescript
// Вместо проверки истечения, просто обновляйте всех на Pro
export const checkExpiredSubscriptions = functions.pubsub
  .schedule('every 24 hours')
  .onRun(async () => {
    const usersSnapshot = await admin.firestore()
      .collection('users')
      .get();

    const batch = admin.firestore().batch();

    usersSnapshot.forEach(doc => {
      batch.update(doc.ref, {
        plan: 'pro',  // Все получают Pro
        status: 'active',
        subscriptionEnd: null  // Без даты окончания
      });
    });

    await batch.commit();
    console.log('All users updated to Pro plan');
  });
```

### Шаг 3: Обновите AuthContext

В файле `src/contexts/AuthContext.tsx` найдите функцию создания пользователя и установите Pro план по умолчанию:

```typescript
// При создании нового пользователя в Firestore:
const newUser = {
  email: user.email,
  name: user.displayName || '',
  plan: 'pro',  // Было 'free'
  status: 'active',
  createdAt: serverTimestamp(),
  // ...остальные поля
};
```

### Шаг 4: Скройте страницу тарифов

В файле `src/App.tsx` (или где у вас роутинг) закомментируйте роут на тарифы:

```typescript
// <Route path="/pricing" element={<PricingPage />} />
```

### Шаг 5: Деплой

```bash
npm run build
firebase deploy --only functions,hosting
```

**Готово!** Теперь все новые пользователи получают Pro план бесплатно.

---

## 🗑️ Вариант 2: Полное удаление (глубокие изменения)

### Шаг 1: Удалите платежные функции

```bash
# Удалите файлы:
rm functions/src/createWayForPayPayment.ts
rm functions/src/wayforpayWebhook.ts
rm WAYFORPAY_SETUP.md

# Обновите functions/src/index.ts:
import * as admin from 'firebase-admin';
admin.initializeApp();

// Удалите все экспорты платежных функций
// Можете даже удалить checkExpiredSubscriptions если не нужна
```

### Шаг 2: Упростите модель пользователя

В Firestore Security Rules (`firestore.rules`) уберите проверки на план:

```javascript
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;

      // Уберите проверки на plan, subscriptionStatus и т.д.
    }
  }
}
```

### Шаг 3: Удалите UI тарифов

```bash
# Найдите и удалите компоненты:
rm src/pages/PricingPage.tsx  # (если есть)
rm src/components/SubscriptionCard.tsx  # (если есть)
```

### Шаг 4: Упростите AuthContext

Удалите поля `plan`, `subscriptionStatus` из интерфейса User:

```typescript
// src/types/user.types.ts или где у вас типы
interface User {
  id: string;
  email: string;
  name: string;
  // Удалите:
  // plan?: string;
  // subscriptionStatus?: string;
  // subscriptionEnd?: Date;
}
```

### Шаг 5: Очистите зависимости

В `functions/package.json` можете убрать ненужные зависимости (если больше не используются):

```json
{
  "dependencies": {
    // Можно убрать body-parser, cors если не используете
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.5.0"
  }
}
```

### Шаг 6: Обновите конфигурацию

Если удалили ВСЕ функции, обновите `firebase.json`:

```json
{
  // Удалите секцию functions если их нет:
  // "functions": { ... },

  "hosting": { ... },
  "firestore": { ... },
  "storage": { ... }
}
```

---

## ⚠️ Важные замечания

### Что нужно проверить после изменений:

1. **Регистрация работает** - новые пользователи создаются
2. **Лимиты не блокируют** - проверьте функции, которые зависели от плана
3. **UI не сломался** - удалите все упоминания тарифов в UI

### Что оставить для будущего:

**НЕ удаляйте:**
- Поля `plan` в Firestore (просто везде ставьте 'pro')
- Документацию `WAYFORPAY_SETUP.md` (сохраните для будущего)
- Типы подписок в коде (просто не используйте)

**Причина:** Когда захотите вернуть платежи, будет проще восстановить.

---

## 🔄 Как вернуть платежи потом

### Если делали Вариант 1 (минимальные изменения):

```bash
# 1. Раскомментируйте функции в functions/src/index.ts
# 2. Верните роут на /pricing
# 3. Измените checkExpiredSubscriptions на проверку даты
# 4. firebase deploy
```

### Если делали Вариант 2 (полное удаление):

```bash
# 1. Восстановите файлы из git:
git checkout HEAD -- functions/src/createWayForPayPayment.ts
git checkout HEAD -- functions/src/wayforpayWebhook.ts

# 2. Добавьте роуты обратно
# 3. Восстановите типы User
# 4. firebase deploy
```

---

## 📊 Экономия ресурсов

### Бесплатный tier Firebase после удаления платежей:

**Было:**
- Cloud Functions: 3 функции (~1-5K вызовов/мес)
- Стоимость: $0 (в пределах лимита)

**Стало (Вариант 1):**
- Cloud Functions: 1 функция (checkExpiredSubscriptions)
- Стоимость: $0

**Стало (Вариант 2):**
- Cloud Functions: 0 функций
- Стоимость: $0
- **Экономия:** ~5-10K function invocations (запас на другие задачи)

---

## 🎯 Рекомендация

**Для MVP выбирайте Вариант 1:**
- ✅ Быстро внедрить (30 минут)
- ✅ Легко вернуть платежи
- ✅ Сохраняете архитектуру
- ✅ Достаточно для 100-500 бета-тестеров

**Вариант 2 подходит если:**
- Платежи не планируете минимум 6 месяцев
- Хотите максимально упростить код
- Уверены что будет только бесплатная версия

---

## ✅ Чеклист

- [ ] Решили какой вариант использовать
- [ ] Сделали резервную копию проекта (git commit)
- [ ] Отключили/удалили платежные функции
- [ ] Обновили AuthContext для Pro плана по умолчанию
- [ ] Скрыли/удалили UI тарифов
- [ ] Протестировали регистрацию
- [ ] Проверили что функции работают (если оставили)
- [ ] Задеплоили изменения
- [ ] Проверили на проде что все работает

---

## 🆘 Если что-то сломалось

```bash
# Откатите изменения:
git reset --hard HEAD

# Или восстановите конкретный файл:
git checkout HEAD -- path/to/file

# Проверьте логи функций:
firebase functions:log

# Проверьте Firestore Rules:
firebase deploy --only firestore:rules
```
